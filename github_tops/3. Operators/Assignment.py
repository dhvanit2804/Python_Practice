'''Used to assign and update variable values.

Operator	Example	    Equivalent to
    =	    x = 5	    Assign value
    +=	    x += 3	    x = x + 3
    -=	    x -= 3	    x = x - 3
    *=	    x *= 3	    x = x * 3
    /=	    x /= 3	    x = x / 3
    //=	    x //= 3	    x = x // 3
    %=      x %= 3	    x = x % 3
    **=     x **= 3	    x = x ** 3'''

x = 10
x += 5
print(x)  # Output: 15
